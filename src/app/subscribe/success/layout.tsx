export const metadata = {
  title: "Subscription Confirmed | Tour de Fore",
  description: "Welcome to the Devils. Your VIP subscription is active.",
  robots: { index: false, follow: false },
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
