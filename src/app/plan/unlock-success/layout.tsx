export const metadata = {
  title: "Plan Unlocked | Tour de Fore",
  description: "Your full trip plan is being generated.",
  robots: { index: false, follow: false },
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
